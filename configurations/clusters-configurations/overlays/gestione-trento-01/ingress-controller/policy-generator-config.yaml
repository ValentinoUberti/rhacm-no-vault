apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: ingress-gestione-trento-01
policyDefaults:
  namespace: policies
  placement:
    clusterSelectors:
      name: 'local-cluster'
  remediationAction: enforce
  complianceType: "musthave"
policies:
  - name: ingress-config-gestione-trento-01
    manifests:
      - path: ingress-controller.yaml 

